const roomFormEdit = rawRoomData => {
    rawRoomData.map(rawRoom => {
        const {
            room_name,
            room_desc,
            num_members,
            members,
            iconlist,
            need_passwd,
            create_time
        } = rawRoom;




        }
    )
}

const a = new Date("2021-07-10 07:05:44 GMT").getTime();
console.log(a);